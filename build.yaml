# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
# - board: seeeduino_xiao_ble
#   shield: urchin_dongle rgbled_adapter
#   snippet: studio-rpc-usb-uart
#   cmake-args: -DCONFIG_ZMK_STUDIO=y
#   artifact-name: urchin_dongle
# - board: nice_nano_v2
#   shield: urchin_left nice_view_adapter  nice_view_gem
#   snippet: studio-rpc-usb-uart
#   cmake-args: -DCONFIG_ZMK_STUDIO=y
# - board: nice_nano_v2
#   shield: urchin_right nice_view_adapter nice_view_gem
# - board: nice_nano_v2
#   shield: settings_reset
include:

    # Urchin
  - board: xiao_ble//zmk
    shield: urchin_dongle dongle_screen
    snippet: zmk-usb-logging
    cmake-args: -DCONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=8000
    artifact-name: xiao_dongle

    # 진단용 대조군: 실드 구성은 그대로 두고 디스플레이만 끈다.
    # 실드를 빼면 config/urchin_dongle.overlay의 &st7789 라벨이 없어 파싱이 깨지므로,
    # dongle_screen은 유지한 채 cmake-args로 디스플레이를 강제로 비활성화한다.
  - board: xiao_ble//zmk
    shield: urchin_dongle dongle_screen
    snippet: zmk-usb-logging
    cmake-args: -DCONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=8000 -DCONFIG_ZMK_DISPLAY=n
    artifact-name: xiao_dongle_nodisplay
  - board: nice_nano@2.0.0
    shield: urchin_left
    cmake-args: -DCONFIG_ZMK_SPLIT=y -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
    artifact-name: urchin_left_peripheral
  - board: nice_nano@2.0.0
    shield: urchin_right
    cmake-args: -DCONFIG_ZMK_SPLIT=y -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
    artifact-name: urchin_right

    # Reset shields
  - board: nice_nano@2.0.0
    shield: settings_reset
  - board: xiao_ble//zmk
    shield: settings_reset
